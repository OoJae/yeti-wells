# Dependencies
node_modules/
.pnpm-store/

# Build output
build/
dist/
target/
**/build/

# Move
move/**/build/

# Env / secrets (NEVER commit keys — Enoki secret, private keys)
.env
.env.local
.env.*.local
*.local
!*.example

# Local tooling / editor
.claude/
.DS_Store
*.log

# Sui keystore safety (never commit private keys)
*.keystore

# regenerated each seed run
scripts/.seeded.json

# browser test artifacts
.playwright-mcp/
yeti-wells-phase*.png

# Nautilus clone (reproducible; our app lives in enclave/yeti_wells_app)
enclave/nautilus/

# Oyster payment wallet key (your money — never commit)
.oyster-wallet

# stray local tooling state / test screenshots
.oyster-deposit-coin
create-page.png
