{
  "name": "yeti-wells-server",
  "private": true,
  "type": "module",
  "description": "Enoki sponsor + starter-grant backend for Yeti Wells",
  "scripts": {
    "dev": "node --env-file=.env --watch index.ts",
    "start": "node --env-file=.env index.ts"
  },
  "dependencies": {
    "@mysten/enoki": "^1.2.0",
    "@mysten/sui": "^2.20.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0"
  }
}
